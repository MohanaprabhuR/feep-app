"use client";

import React from "react";

// Policies routes reuse the auth + header logic from the parent (private) layout.
export default function PoliciesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}


